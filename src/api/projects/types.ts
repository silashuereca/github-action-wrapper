export type TProject = {
  created_at: string;
  description: string;
  id: string;
  name: string;
  project_id: string;
  repo: string;
  status: string; //nothing defined yet
  updated_at: string;
  workflow_data: any; //any for now until structure is defined
};
